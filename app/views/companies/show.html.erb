


<div class="h5 my-5 border px-5">
    <div class="row align-items-center my-3 border">
        <div class="col-md-2 text-center">
            <% if @company.logo && @company.logo.attached? %>
                <%= image_tag @company.logo.variant(resize: "100x100"), class: "p-3" %> 
            <% else %>
                <%= image_tag "https://picsum.photos/100/100", class: "p-3"%>
            <% end %>
            <p ><%= @company.season %>기</p>
        </div>
        <div class="col-md-10 ">
            <div class="row">
                <div class="col"><p>상호명 : <%= @company.name %></p></div>
                <div class="col"><p>상호명(영문) : <%= @company.name_en %></p></div>
            </div>
            <div class="row" >
                <div class="col"><p>서비스명 : <%= @company.service_name %></p></div>
                <div class="col"><p>서비스명(영문) : <%= @company.service_name_en %></p></div>
            </div>
        </div>
    </div>
   
    <div class="row my-3">
        <div class="col-md-12"><p>기업 설립 날짜 : <%= @company.established_date%></p></div>
    </div>
    <div class="row my-3">
        <div class="col-md-12">
            <p>사업자 등록 번호 : 
                <% if @company.is_registered %>
                    <%= @company.register_num%>
                <% else %>
                    미등록
                <% end %>
            </p>
        </div>
    </div>
    <div class="row my-3">

        <div class="col-md-5">
            <p>법인 등록 번호 : 
                <% if @company.is_corporated %>
                    <%= @company.corporated_num%>
                <% else %>
                    미등록
                <% end %>
            </p>
        </div>
        
        <% if @company.is_corporated %>
            <div class="col-md-5"><p>법인 설립 날짜 : <%= @company.corporated_date %></p></div>
        <% end %>
    </div>
    <div class="row my-3">
        <div class="col-md-3">
        </div>
        <div class="col-md-9"><p>대표자명 : <%= @company.CEO1 %></p></div>
    </div>

    <div class="row my-3">
        <div class="col-md-12"><p>주소 : 서울특별시 성북구 안암로 145 경영본관 2층 <%= @company.address %>호</p></div>
    </div>

    <div class="row my-3">
        <div class="col-md-12">
            <p>기업 개요</p>
            <div style="word-break:break-all; height:250px; overflow:scroll" class="border p-3"><p><%= @company.company_overview %></p></div>
        </div>
    </div>
    <div class="row my-3">
        <div class="col-md-12">
            <p>서비스 개요</p>
            <div style="word-break:break-all; height:250px; overflow:scroll" class="border p-3"><p><%= @company.service_overview %></p></div>
        </div>
    </div>

    <div class="row my-3">
        <div class="col-md-6">
            <p>웹사이트 : <%= @company.website %></p>
        </div>
        <div class="col-md-6">
            <p>SNS : <%= @company.sns %></p>
        </div>
    </div>

    <div class="row my-3">
        <div class="col-md-4">
            <% if @company.cellphone_num %>
            <p>휴대폰 번호 : <%= @company.cellphone_num[0,3] %>-<%= @company.cellphone_num[3,4] %>-<%= @company.cellphone_num[7,4] %></p>
            <% end %>
        </div>
        <div class="col-md-4">
            <p>일반전화 번호 : <%= @company.phone_num %></p>
        </div>
    </div>

    <div class="row my-3">
        <div class="col-md-6">
            <p>입주 날짜 : <%= @company.move_in_date %></p>
        </div>
        <div class="col-md-6">
            <p>퇴주 날짜 : <%= @company.move_out_date %></p>
        </div>
    </div>

    <div class="row my-3">
        <div class="col-md-6"><p>경영대생 유무 :
            <% if @company.is_business_student == true %>
                O
            <% else %>
                X
            <% end%>
        </p></div>
    </div>

    <div class="my-3 d-flex justify-content-end">
        <div>
            <% if @company.express_file.attachment != nil %>
                <a class="m-2 btn btn-outline-info" href="<%= @company.express_file.service_url %>" target="_blank">EXPRESS 자료</a>
            <% else %>
                <a class="m-2 btn btn-outline-info disabled" ><del>EXPRESS 자료</del></a>
            <% end %>
        </div>
        <div>
            <% if @company.presentation.attachment != nil %>
                <a class="m-2 btn btn-outline-info" href="<%= @company.presentation.service_url %>" target="_blank">발표 자료</a>
            <% else %>
                <a class="m-2 btn btn-outline-info disabled" ><del>발표 자료</del></a>
            <% end %>
        </div>
        <div>
            <% if @company.logo.attachment != nil %>
                <a class="m-2 btn btn-outline-info" href="<%= @company.logo.service_url %>" target="_blank">기업 로고</a>
            <% else %>
                <a class="m-2 btn btn-outline-info disabled" ><del>기업 로고</del></a>
            <% end %>
        </div>
        <div>
            <% if @company.logo_ai.attachment != nil %>
                <a class="m-2 btn btn-outline-info" href="<%= @company.logo_ai.service_url %>" target="_blank">기업 로고(Ai)</a>
            <% else %>
                <a class="m-2 btn btn-outline-info disabled" ><del>기업 로고(Ai)</del></a>
            <% end %>
        </div>
    </div>
    <div>
        <%= link_to "수정하기", edit_company_path(@company), class: "mb-5 btn btn-light"%>
    </div>
</div>
