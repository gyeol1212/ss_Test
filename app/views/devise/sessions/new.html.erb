<div class="row">
<div class="col-md-8">

<%= image_tag("Main_Image.jpg", style:"width: 100%; height: 120%") %>
</div>


<div class="col-md-4 align-self-center">
  <h2 class="text-center">Log in</h2>

  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div>
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { :class => "form-control", autocomplete: "email" }%>
      <%= f.input :password,
                  required: false,
                  input_html: { :class => "form-control", autocomplete: "current-password" } %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Log in", class: "btn btn-light" %>
      <%= render "devise/shared/links" %>
    </div>
  <% end %>



  <%= link_to "비회원 접속", visitor_path,class: 'btn btn-light'%>
<div>